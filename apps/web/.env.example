# Vite reads this file, not the repository root: its env directory is the
# package that holds vite.config.ts. Both keys have working defaults in source,
# so local development runs without this file; deployments set them.
VITE_API_URL=http://localhost:3001
VITE_SENTRY_DSN=
